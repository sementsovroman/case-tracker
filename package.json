{
  "name": "calendar-prototype",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "build:railway": "npm run build --workspace frontend && rm -rf backend/public && mkdir -p backend/public && cp -R frontend/dist/* backend/public/ && npm run build --workspace backend && npx prisma generate --schema backend/prisma/schema.prisma"
  }
}